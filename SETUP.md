# دليل التركيب — من الجوال بالكامل

ما تحتاجش حاسوب. كل الخطوات من متصفح جوالك.
الوقت المتوقع: **30–40 دقيقة**. التكلفة: **صفر**.

---

## الخطوة 1 — أنشئ بوت تليجرام (3 دقائق)

1. افتح تليجرام وابحث عن **@BotFather**
2. ابعتله `/newbot`
3. اختر اسماً للبوت (مثلاً: سند)
4. اختر معرّفاً ينتهي بـ `bot` (مثلاً: `sanad_office_bot`)
5. بيعطيك **رمزاً** بهذا الشكل: `8123456789:AAF...`

📝 **احفظ الرمز** — بنسمّيه `TELEGRAM_BOT_TOKEN`

---

## الخطوة 2 — احصل على مفتاح Gemini (دقيقتان)

1. افتح <https://aistudio.google.com/apikey>
2. سجّل دخول بحساب جوجل
3. اضغط **Create API key**

📝 **احفظ المفتاح** — بنسمّيه `GEMINI_API_KEY`

---

## الخطوة 3 — أنشئ حساب Cloudflare (3 دقائق)

1. افتح <https://dash.cloudflare.com/sign-up>
2. سجّل ببريدك — **ما يطلبش بطاقة**
3. أكّد البريد

---

## الخطوة 4 — أنشئ قاعدة البيانات (3 دقائق)

من لوحة Cloudflare:

1. من القائمة الجانبية: **Storage & Databases** ← **D1**
2. اضغط **Create database**
3. الاسم: `sanad` ← **Create**

---

## الخطوة 5 — أنشئ الـ Worker (5 دقائق)

1. من القائمة: **Compute (Workers)** ← **Workers & Pages**
2. **Create** ← **Start with Hello World** ← **Deploy**
3. بعد النشر اضغط **Edit code** (أو **`</>` Edit**)
4. **امسح كل الكود الموجود**، والصق محتوى ملف `worker.js` كامل
5. اضغط **Deploy**

📝 احفظ رابط الـ Worker، شكله:
`https://اسم-الوركر.اسمك.workers.dev`

---

## الخطوة 6 — اربط الإعدادات (7 دقائق)

من صفحة الـ Worker ← **Settings**

### أ. اربط قاعدة البيانات

**Bindings** ← **Add** ← **D1 database**

| الحقل | القيمة |
|---|---|
| Variable name | `DB` ← **لازم بالضبط كذا** |
| D1 database | `sanad` |

### ب. أضف المتغيرات

**Variables and Secrets** ← **Add** لكل واحد من هذول:

| الاسم | القيمة | النوع |
|---|---|---|
| `TELEGRAM_BOT_TOKEN` | رمز BotFather | Secret |
| `GEMINI_API_KEY` | مفتاح Gemini | Secret |
| `SETUP_SECRET` | **كلمة سر تخترعها أنت** (مثلاً `sanad-2026-xyz`) | Secret |
| `UTC_OFFSET_MINUTES` | `120` (ليبيا) | Text |

> ⚠️ `SETUP_SECRET` هي مفتاح الأمان لكل شيء: بيها تدخل صفحة الإعداد، وبيها تسجّل نفسك كمالك. اخترع كلمة طويلة صعبة التخمين واحفظها.

### ج. فعّل المؤقّت (للتذكيرات)

**Settings** ← **Triggers** ← **Cron Triggers** ← **Add**
الصيغة: `* * * * *` (كل دقيقة)

ثم ارجع لـ **Deployments** واضغط **Redeploy** حتى تُطبَّق الإعدادات.

---

## الخطوة 7 — شغّل الإعداد (3 دقائق)

افتح في متصفح جوالك:

```
https://رابط-الوركر/setup?key=كلمة_السر_تاعتك
```

بتشوف صفحة فيها حالة كل شيء. اضغط الأزرار بالترتيب:

1. **إنشاء قاعدة البيانات**
2. **تفعيل بوت تليجرام**

---

## الخطوة 8 — سجّل نفسك كمالك (دقيقة)

1. افتح البوت في تليجرام (المعرّف اللي اخترته في الخطوة 1)
2. اضغط **Start**
3. ابعتله **كلمة السر** (`SETUP_SECRET`) كرسالة عادية

بيرد عليك: *"تم تسجيلك كمالك للبوت"* ✅

**البوت شغّال توا.** جرّب تبعتله سؤال أو رسالة صوتية.

---

## الخطوة 9 — اربط جوجل (اختياري، 10 دقائق)

بدونها البوت يشتغل ويجاوب ويذكّرك، لكن **بدون تقويم ولا درايف**.

### أ. أنشئ مشروعاً

1. <https://console.cloud.google.com/> ← **New Project** ← سمّه `sanad`
2. **APIs & Services** ← **Library** ← فعّل:
   - **Google Calendar API**
   - **Google Drive API**

### ب. شاشة الموافقة

**OAuth consent screen**:
- User Type: **External**
- App name: `sanad`
- البريد: بريدك
- في **Test users** ← **أضف بريدك** ⚠️ ضروري، وبدونه بيرفض الدخول

### ج. بيانات الاعتماد

**Credentials** ← **Create Credentials** ← **OAuth client ID**

| الحقل | القيمة |
|---|---|
| Application type | **Web application** |
| Authorized redirect URIs | `https://رابط-الوركر/auth/callback` |

📝 احفظ `Client ID` و`Client Secret`

### د. أضفهم في Cloudflare

| الاسم | النوع |
|---|---|
| `GOOGLE_CLIENT_ID` | Text |
| `GOOGLE_CLIENT_SECRET` | Secret |

ثم **Redeploy**.

### هـ. اربط الحساب

افتح `https://رابط-الوركر/setup?key=كلمة_السر` ← اضغط **ربط جوجل** ← وافق على الأذونات.

> إذا ظهر تحذير "Google hasn't verified this app" اضغط **Advanced** ← **Go to sanad (unsafe)**. هذا طبيعي لأن التطبيق تاعك ولسه في وضع الاختبار.

### و. مجلد درايف (اختياري)

أنشئ مجلداً في درايف، افتحه، وخذ المعرّف من الرابط:

```
drive.google.com/drive/folders/1AbCdEfGhIjKlMnOp
                               └───── هذا هو ─────┘
```

أضفه كمتغير نصي `DRIVE_FOLDER_ID`. بهذا البوت يحفظ ويبحث داخل المجلد هذا فقط — أنظف وأأمن.

---

## الخطوة 10 — ضيف الـ5 أشخاص

في تليجرام ابعت للبوت: `/دعوة`

بيعطيك كوداً. ابعت الكود للشخص، وخلّيه يفتح البوت ويبعت الكود كرسالة.
الكود صالح **24 ساعة ومرة وحدة بس**.

---

## جاهز ✅

جرّب هذي:

```
"شن عندي بكرة؟"
"سجلي موعد مع المحامي الخميس 4 العصر"
"ذكرني كل يوم الساعة 7 الصبح بالدوا"
"ابعتلي ملف الفاتورة"
"لخصلي الملف هذا"        ← مع إرفاق PDF
```

وابعتله رسالة صوتية وصورة وشوف شن يعمل.

---

## الأوامر

| الأمر | الوظيفة |
|---|---|
| `/دعوة` | كود دعوة لشخص جديد (للمالك) |
| `/القائمة` | عرض المصرّح لهم (للمالك) |
| `/حذف <رقم>` | إزالة شخص (للمالك) |
| `/حالة` | حالة البوت والربط |
| `/مسح` | مسح ذاكرة المحادثة |
| `/مساعدة` | قائمة الأوامر |

---

## إذا صار خلل

| المشكلة | السبب والحل |
|---|---|
| البوت ما يردش | افتح صفحة `/setup` وشوف شن ❌ |
| "غير مصرّح" في صفحة الإعداد | `key` في الرابط مش مطابق لـ `SETUP_SECRET` |
| فشل إنشاء قاعدة البيانات | الربط اسمه مش `DB` بالضبط، أو ما عملتش Redeploy |
| ما تسجّلتش كمالك | تأكد إنك بعت كلمة السر نفسها بالضبط بدون مسافات زايدة |
| التذكيرات ما توصلش | مؤقّت الـ Cron مش مضاف، أو ما عملتش Redeploy بعده |
| جوجل ما يشتغلش | بريدك مش مضاف في **Test users** |
| لمتابعة الأخطاء | من صفحة الـ Worker: **Logs** ← **Begin log stream** |

---

## التعديل لاحقاً

- **شخصية البوت ولهجته:** عدّل قسم `7. شخصية السكرتير` في `worker.js` — خصوصاً الأمثلة، هي أقوى أداة لضبط النبرة. كل ما كانت قريبة من طريقة كلامك الحقيقية، كل ما كان الرد طبيعي أكثر.
- **تعليمات دائمة:** أضف متغيراً نصياً `EXTRA_INSTRUCTIONS` بأي تعليمات تريدها (مثلاً: "ما تسجّلش مواعيد يوم الجمعة").
- **اسم البوت:** متغير `BOT_NAME`.
- **الصلاحيات:** عدّل ثابت `PERMISSIONS` في أول `worker.js` — يحدد شن يقدر يعمله العضو مقابل المالك.

بعد أي تعديل على الكود: **Deploy**.

---

## الحدود المجانية

| الخدمة | الحد |
|---|---|
| Cloudflare Workers | 100,000 طلب يومياً |
| Cloudflare D1 | 5 جيجابايت تخزين |
| Gemini | حد يومي سخي على النماذج السريعة |
| تليجرام | بلا حدود عملياً |

للاستعمال الشخصي مع 6 أشخاص، أنت بعيد جداً عن أي حد من هذول.

> ⚠️ الحدود المجانية تتغيّر مع الوقت. إذا شفت رسائل رفض متكررة، تحقق من صفحة الأسعار للخدمة المعنية.
